[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "VERPO-AGENT"
version = "0.1.0"
description = "Agent OPSD experiment workspace and pinned PGR-Probe integration"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
include = ["verpo_agent*"]
