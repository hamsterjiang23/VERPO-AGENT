# Third-party sources

`third_party/PGR-Probe` retains its independent Git history and all existing file-level notices. In particular, its `verl/` tree includes the veRL Apache-2.0 license and notices. This workspace does not replace or relicense upstream code.

The research Markdown snapshots in `docs/research/` originate from the pinned PGR-Probe commit documented in `provenance.json`. Relative links are rewritten for standalone reading; sources remain attributed. Linked papers are external references, not copied paper PDFs or datasets.

No new repository-wide license is selected by this bootstrap. Check the applicable upstream file licenses before redistribution.
